# 🎬 PlayerokAPI

**PlayerokAPI** — это неофициальная Python-библиотека (API-клиент) для взаимодействия с сайтом playerok.com. Она предоставляет удобный программный доступ для поиска и получения информации о доступных аккаунтах (аккаунтах цифровых магазинов, подписках и т.д.).

## ⚠️ Важное предупреждение / Disclaimer

Данная библиотека создана **в образовательных и исследовательских целях**. Использование API сторонних сайтов должно осуществляться в соответствии с их **Пользовательским соглашением (Terms of Service)**.

*   Автор библиотеки не несет ответственности за ваши действия с использованием этого кода.
*   Настоятельно рекомендуется использовать библиотеку этично и не нарушать работу сервиса playerok.com.
*   Проверяйте легальность и безопасность покупок на сторонних площадках самостоятельно.

## ✨ Возможности

*   **Поиск товаров:** Гибкий поиск по названию игры, сервиса или продукта.
*   **Парсинг данных:** Получение структурированной информации о найденных товарах:
    *   Название
    *   Цена
    *   Ссылка на товар
    *   Платформа (Steam, Origin, Xbox Game Pass и др.)
    *   Регион аккаунта
    *   Дополнительные теги и информация
*   **Автоматизация:** Легко интегрируется в ваши скрипты для мониторинга цен или наличия товаров.
*   **Простота:** Чистый и понятный Python-интерфейс.

## 🚀 Установка

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/zlafik1/playerokapi.git
    cd playerokapi
    ```

2.  **Установите зависимости:** Основная зависимость — `requests`.
    ```bash
    pip install requests
    # или, если используете файл requirements.txt
    pip install -r requirements.txt
    ```

## 📦 Быстрый старт

Вот простой пример, как начать работу с библиотекой:

```python
from playerokapi import PlayerokAPI

# Создаем экземпляр клиента
client = PlayerokAPI()

# Выполняем поиск (например, ищем "Steam Argentina")
search_results = client.search("Steam Argentina")

# Выводим результаты
if search_results:
    print(f"Найдено товаров: {len(search_results)}\n")
    for product in search_results:
        print(f"🎮 Название: {product.get('name', 'Н/Д')}")
        print(f"💰 Цена: {product.get('price', 'Н/Д')} руб.")
        print(f"🔗 Ссылка: {product.get('link', 'Н/Д')}")
        print(f"🏷️ Платформа: {product.get('platform', 'Н/Д')}")
        print(f("📍 Регион: {product.get('region', 'Н/Д')}"))
        print("-" * 40)
else:
    print("По вашему запросу ничего не найдено.")
